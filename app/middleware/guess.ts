export default function (data:any) {
    console.log(data.store);
    //return data.redirect("/")
    /*console.log(data);
    if (process.client && !data.store.getters["isLoggedIn"]) return data.redirect("/login");*/
  }